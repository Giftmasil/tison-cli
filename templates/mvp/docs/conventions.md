# Conventions

Lean conventions for this MVP. Only keep rules that aren't obvious from the
code or already enforced by your linter/formatter. Delete anything that is.

## Naming

- [TODO(tison): casing rules, e.g. files kebab-case, components PascalCase, vars camelCase]

## Structure

- [TODO(tison): where code goes, e.g. feature folders under src/features/<name>]
- Co-locate related code. Don't build deep folder trees for an MVP.

## Errors

- [TODO(tison): how errors are handled, e.g. throw typed errors; never swallow silently]

## Git

- Small commits. Present-tense messages ("add login", not "added login").
- [TODO(tison): branch/PR rules if any — otherwise delete this section]

---

If a rule here is obvious or your tooling already enforces it, remove it.
Short files get followed; long ones get ignored.
